/** Placeholder — full clients UI is out of scope; upload can create a demo client. */

export default function ClientsPage() {
  return (
    <main>
      <h1 className="text-xl font-semibold">Clients</h1>
      <p className="mt-2 text-sm text-stone-600">
        Use Upload to pick or create a client for the core loop.
      </p>
    </main>
  );
}
