# FinDraft frontend image — placeholder. Implementation pending.
FROM node:20-alpine AS base

WORKDIR /app

COPY frontend/package*.json ./
RUN npm ci

COPY frontend/ .
RUN npm run build

CMD ["npm", "start"]
